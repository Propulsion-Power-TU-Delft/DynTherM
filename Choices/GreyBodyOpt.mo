within ThermalManagement.Choices;
type GreyBodyOpt = enumeration(
    Greybody
           "Grey body (eps = abs)",
    notGreybody
              "Not grey body")
"Menu choices to select the material properties used for radiative heat transfer calculations";
